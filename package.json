{
  "name": "fight-scorer-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local server for Fight Scorer — serves the tool and proxies AI calls to Mistral, keeping the API key server-side only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
